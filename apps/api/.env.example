PORT=4000
WEB_ORIGIN=http://localhost:3000
# live | chunked | mock
TRANSCRIBER=mock
# gemini | mock
TRANSLATOR=mock
GEMINI_API_KEY=
# Live speech-to-text model (checked 2026-09-25). Text model used for translation and the chunked fallback.
GEMINI_LIVE_MODEL=gemini-3.5-transcribe-live
GEMINI_TEXT_MODEL=gemini-2.5-flash
MAX_SESSIONS=12
SAMPLES_DIR=../../samples
